
slicer_rc = configure_file(
  input : 'PrusaSlicer.rc.in',
  output: 'PrusaSlicer.rc',
  configuration: config,
)
slicer_manifest = configure_file(
  input : 'PrusaSlicer.manifest.in',
  output: 'PrusaSlicer.manifest',
  configuration: config,
)

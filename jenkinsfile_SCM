pipeline
{
	agent any
	stages 
	{
		stage('Preparation')
		{
			steps
			{
				echo 'Vytvoření složky build'
				sh 'mkdir build'
			}
		}
		stage('Build')
		{
			steps
			{
				sh 'pwd'
				sh 'cd build'
				echo 'První build'
			}
		}
	}
}

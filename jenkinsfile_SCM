pipeline
{
	agent any
	stages 
	{
		stage('Prepare')
		{
			steps
			{
				echo 'Vytvoření složky build'
				pwd
				mkdir -p build
				cd build
				echo 'build 1'

			}
		}
		stage('Dependency_resolution')
		{
			steps
			{

			}
		}
	}
}

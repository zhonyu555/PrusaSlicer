pipeline
{
	agent any
	stages 
	{
		stage('Preparation')
		{
			steps
			{
				echo 'Vytvoření složky build'
				sh 'mkdir -p build'
			}
		}
		stage('Build')
		{
			steps
			{
				echo 'První build'
				cmake(installation: 'cmake', arguments: '-GNinja ./')
			}
		}
	}
}
